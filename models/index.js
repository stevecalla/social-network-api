const User = require('./User');
const Thought = require('./Thought');
const Tag = require('./Tag');
const Application = require('./Application');

module.exports = { User, Thought, Application, Tag };
